<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>记录一次startUML破解</title>
    <url>/2019/12/02/%E8%AE%B0%E5%BD%95%E4%B8%80%E6%AC%A1startUML%E7%A0%B4%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="qian-yan">前言<a href="#qian-yan" class="header-anchor" target="_blank" aria-hidden="true">#</a></h1><p>之前startUML还在3.1.0的时候其实已经破解过一次了，网上的破解教程其实也有很多。讲道理现在startUML的颜值还是很高的。startUML目前的版本都是通过electron打包生成的软件，纯前端开发的东西，并且代码清晰，该有注释有注释，业务逻辑一点混淆都没做，搞得我破解的怪不好意思的。我没有搞事情！<img src="https://i.loli.net/2019/12/02/VeT3DX9QKjMd1rN.jpg" alt>但是因为最近StartUML 3.1.1版本发布，发现退出时会强制弹出更新，怪难受的，上次没注意选了是，结果瞬间给我装完了。更加增加了我去除更新的决心。</p>
<a id="more"></a>

<h1 id="po-jie-bu-zou">破解步骤<a href="#po-jie-bu-zou" class="header-anchor" target="_blank" aria-hidden="true">#</a></h1><h2 id="0x0-qian-zhi-gong-zuo">0x0.前置工作<a href="#0x0-qian-zhi-gong-zuo" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>1.首先电脑应该安装node环境，这我就不介绍了，各位自行下载。<br>2.安装asar包，为后面的解压app.asar做准备<code>npm install asar -g</code>，<code>-g</code>参数是全局安装，node的工作机制我也不介绍了（嘘，其实是我不懂，就是一个弱鸡前端）。<br>3.找到startUML安装目录，找到resources文件夹，里面的app.asar就是我们的目标了。<br>4.打开命令行使用<code>npm extract app.asar app</code>将app.asar的文件解压到app文件夹。</p>
<h2 id="0x1-po-jie-license-xiao-yan">0x1.破解license校验<a href="#0x1-po-jie-license-xiao-yan" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>license校验的文件就在<code>app/src/engine/license-manager.js</code>中，打开文件我们可以看到这样一段代码:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Check license validity</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @return &#123;Promise&#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">validate () &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// Local check</span></span><br><span class="line">      <span class="keyword">var</span> file = <span class="keyword">this</span>.findLicense()</span><br><span class="line">      <span class="keyword">if</span> (!file) &#123;</span><br><span class="line">        reject(<span class="string">'License key not found'</span>)</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">var</span> data = fs.readFileSync(file, <span class="string">'utf8'</span>)</span><br><span class="line">        licenseInfo = <span class="built_in">JSON</span>.parse(data)</span><br><span class="line">        <span class="keyword">var</span> base = SK + licenseInfo.name +</span><br><span class="line">          SK + licenseInfo.product + <span class="string">'-'</span> + licenseInfo.licenseType +</span><br><span class="line">          SK + licenseInfo.quantity +</span><br><span class="line">          SK + licenseInfo.timestamp + SK</span><br><span class="line">        <span class="keyword">var</span> _key = crypto.createHash(<span class="string">'sha1'</span>).update(base).digest(<span class="string">'hex'</span>).toUpperCase()</span><br><span class="line">        <span class="keyword">if</span> (_key !== licenseInfo.licenseKey) &#123;</span><br><span class="line">          reject(<span class="string">'Invalid license key'</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="comment">// Server check</span></span><br><span class="line">          $.post(app.config.validation_url, &#123;<span class="attr">licenseKey</span>: licenseInfo.licenseKey&#125;)</span><br><span class="line">            .done(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">              resolve(data)</span><br><span class="line">            &#125;)</span><br><span class="line">            .fail(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">              <span class="keyword">if</span> (err &amp;&amp; err.status === <span class="number">499</span>) &#123; <span class="comment">/* License key not exists */</span></span><br><span class="line">                reject(err)</span><br><span class="line">              &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// If server is not available, assume that license key is valid</span></span><br><span class="line">                resolve(licenseInfo)</span><br><span class="line">              &#125;</span><br><span class="line">            &#125;)</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">      reject(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">checkLicenseValidity () &#123;</span><br><span class="line">  <span class="keyword">this</span>.validate().then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    setStatus(<span class="keyword">this</span>, <span class="literal">true</span>)</span><br><span class="line">  &#125;, () =&gt; &#123;</span><br><span class="line">    setStatus(<span class="keyword">this</span>, <span class="literal">false</span>)</span><br><span class="line">    UnregisteredDialog.showDialog()</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>很显然这就是我们本次的目标惹！但是网上几乎全部的破解的步骤都是只改了2行代码:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">checkLicenseValidity () &#123;</span><br><span class="line">  <span class="keyword">this</span>.validate().then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    setStatus(<span class="keyword">this</span>, <span class="literal">true</span>)</span><br><span class="line">  &#125;, () =&gt; &#123;</span><br><span class="line">    <span class="comment">// 去除失败部分的逻辑</span></span><br><span class="line">    <span class="comment">// setStatus(this, false)</span></span><br><span class="line">    <span class="comment">// UnregisteredDialog.showDialog()</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 强行将失败部分逻辑的状态设置成与success部分一致</span></span><br><span class="line">    setStatus(<span class="keyword">this</span>, <span class="literal">true</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然这样确实so easy的就将startUML的license校验给干掉了，但是主动输入密钥的话还是会提示秘钥无效，破解的一点都不完整，这才不是我想要的破解！<img src="https://i.loli.net/2019/12/02/OsFe1HX45vhkY9t.jpg" alt>（PS：小声逼逼，真的没见过比这个更容易的破解了）</p>
<p>我们现在分析一下其余部分的代码</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">validate () &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// Local check</span></span><br><span class="line">        <span class="comment">// 注释写着本地判断，如果license文件不存在则直接返回license无效的异常，否则则进入下一段逻辑</span></span><br><span class="line">        <span class="keyword">var</span> file = <span class="keyword">this</span>.findLicense()</span><br><span class="line">        <span class="keyword">if</span> (!file) &#123;</span><br><span class="line">          reject(<span class="string">'License key not found'</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="comment">// 读取license文件</span></span><br><span class="line">          <span class="keyword">var</span> data = fs.readFileSync(file, <span class="string">'utf8'</span>)</span><br><span class="line">          licenseInfo = <span class="built_in">JSON</span>.parse(data)</span><br><span class="line">          <span class="comment">// 将信息拼接字符串</span></span><br><span class="line">          <span class="keyword">var</span> base = SK + licenseInfo.name +</span><br><span class="line">            SK + licenseInfo.product + <span class="string">'-'</span> + licenseInfo.licenseType +</span><br><span class="line">            SK + licenseInfo.quantity +</span><br><span class="line">            SK + licenseInfo.timestamp + SK</span><br><span class="line">          <span class="comment">// 字符串哈希并且转换大写</span></span><br><span class="line">          <span class="keyword">var</span> _key = crypto.createHash(<span class="string">'sha1'</span>).update(base).digest(<span class="string">'hex'</span>).toUpperCase()</span><br><span class="line">          <span class="comment">// 如果本地校验失败则不通过，否则进入服务器请求逻辑</span></span><br><span class="line">          <span class="keyword">if</span> (_key !== licenseInfo.licenseKey) &#123;</span><br><span class="line">            reject(<span class="string">'Invalid license key'</span>)</span><br><span class="line">          &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// Server check</span></span><br><span class="line">            <span class="comment">// 服务器判断</span></span><br><span class="line">            $.post(app.config.validation_url, &#123;<span class="attr">licenseKey</span>: licenseInfo.licenseKey&#125;)</span><br><span class="line">              .done(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">                resolve(data)</span><br><span class="line">              &#125;)</span><br><span class="line">              .fail(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">                <span class="comment">// 注释说的很清楚了，如果status === 499则代表license无效</span></span><br><span class="line">                <span class="keyword">if</span> (err &amp;&amp; err.status === <span class="number">499</span>) &#123; <span class="comment">/* License key not exists */</span></span><br><span class="line">                  reject(err)</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                  <span class="comment">// If server is not available, assume that license key is valid</span></span><br><span class="line">                  <span class="comment">// 如果没有返回异常并且返回值 status !== 499，则通过校验</span></span><br><span class="line">                  resolve(licenseInfo)</span><br><span class="line">                &#125;</span><br><span class="line">              &#125;)</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">        reject(err)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>仔细分析一下代码，我们可以得知到了所有的判断逻辑，那么我们只需要将服务器相关的逻辑干掉即可，只保留读取文件以及回调的逻辑：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">validate () &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="comment">// Local check</span></span><br><span class="line">      <span class="keyword">var</span> file = <span class="keyword">this</span>.findLicense()</span><br><span class="line">      <span class="keyword">if</span> (!file) &#123;</span><br><span class="line">        reject(<span class="string">'License key not found'</span>)</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">var</span> data = fs.readFileSync(file, <span class="string">'utf8'</span>)</span><br><span class="line">        licenseInfo = <span class="built_in">JSON</span>.parse(data)</span><br><span class="line">        resolve(licenseInfo)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">      reject(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后再将licenseKey注册部分逻辑解决即可，这是原逻辑：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Check the license key in server and store it as license.key file in local</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * @param &#123;string&#125; licenseKey</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">register (licenseKey) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    $.post(app.config.validation_url, &#123;<span class="attr">licenseKey</span>: licenseKey&#125;)</span><br><span class="line">      .done(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">var</span> file = path.join(app.getUserPath(), <span class="string">'/license.key'</span>)</span><br><span class="line">        fs.writeFileSync(file, <span class="built_in">JSON</span>.stringify(data, <span class="number">2</span>))</span><br><span class="line">        licenseInfo = data</span><br><span class="line">        setStatus(<span class="keyword">this</span>, <span class="literal">true</span>)</span><br><span class="line">        resolve(data)</span><br><span class="line">      &#125;)</span><br><span class="line">      .fail(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">        setStatus(<span class="keyword">this</span>, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">if</span> (err.status === <span class="number">499</span>) &#123; <span class="comment">/* License key not exists */</span></span><br><span class="line">          reject(<span class="string">'invalid'</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          reject()</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这部分逻辑就是注册license的逻辑了，逻辑理解没有任何难度，是不是还记得上面有一段哈希的操作我们照着键值原样仿制一个license的json对象即可：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">register (licenseKey) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">var</span> data = &#123;</span><br><span class="line">          name: <span class="string">"Teble"</span>,</span><br><span class="line">          product: <span class="string">"Teble product"</span>,</span><br><span class="line">          licenseType: <span class="string">"PS"</span>,</span><br><span class="line">          quantity: <span class="string">"Teble Quantity"</span>,</span><br><span class="line">          timestamp: <span class="string">"1575275098"</span>,</span><br><span class="line">          licenseKey: <span class="string">"It's Cracked!!"</span>,</span><br><span class="line">          crackedAuthor: <span class="string">"Teble"</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">var</span> file = path.join(app.getUserPath(), <span class="string">'/license.key'</span>)</span><br><span class="line">      fs.writeFileSync(file, <span class="built_in">JSON</span>.stringify(data, <span class="number">2</span>))</span><br><span class="line">      licenseInfo = data</span><br><span class="line">      setStatus(<span class="keyword">this</span>, <span class="literal">true</span>)</span><br><span class="line">      resolve(data)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>破解到这里可以基本告一段落了，软件是能使用了，但是更新的问题还没解决呢，如果startUML检测到版本有更新，或者手动误操作点击了更新，依旧是会进行后台下载不需要经过你二次同意的。</p>
<h2 id="0x2-qu-chu-qi-dong-geng-xin-jian-ce">0x2.去除启动更新检测<a href="#0x2-qu-chu-qi-dong-geng-xin-jian-ce" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>这部分逻辑比我想象中的难找了一点，我还纳闷了一会，他到底是怎么做到关闭软件时再弹出安装操作的，然后经历了几次卸载，观察发现，当软件开启时就会检测是否有更新，如果有则立马开启后台下载，网速嗖嗖嗖的飚，路径下载到了<code>AppData\Roaming\StarUML</code>这个路径下，下载完成后，如果关闭软件，则会立马触发安装操作。</p>
<p>这部分逻辑在<code>app/src/main-process/application.js</code>文件下注释一句：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Application</span> <span class="keyword">extends</span> <span class="title">EventEmitter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">constructor</span> () &#123;</span><br><span class="line">    <span class="keyword">super</span>()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">this</span>.loadExtensions()</span><br><span class="line">    <span class="keyword">this</span>.handleCommands()</span><br><span class="line">    <span class="keyword">this</span>.handleMessages()</span><br><span class="line">    <span class="keyword">this</span>.handleEvents()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Check for updates on start</span></span><br><span class="line">    <span class="comment">// 启动时判断更新，注释就完事了</span></span><br><span class="line">    <span class="comment">//autoUpdater.checkForUpdatesAndNotify()</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此外engin目录下还有一个<code>update-manager.js</code>，注释后，手动检测更新依旧返回已经是最新版无需更新的字样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Update Manager</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UpdateManager</span> <span class="keyword">extends</span> <span class="title">EventEmitter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">constructor</span> () &#123;</span><br><span class="line">    <span class="keyword">super</span>()</span><br><span class="line">    <span class="keyword">this</span>.state = <span class="string">'no-update'</span></span><br><span class="line">    <span class="keyword">this</span>.updateInfo = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">this</span>.progress = <span class="literal">null</span></span><br><span class="line">    <span class="comment">// 注释这一句屏蔽手动更新检测</span></span><br><span class="line">    <span class="comment">//this.handleMessages()</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="0x3-po-jie-wan-cheng">0x3.破解完成<a href="#0x3-po-jie-wan-cheng" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>现在我们已经获得了破解完成，最后我们在根目录使用<code>asar pack app app.asar</code>将app文件夹打包成asar文件，最后将文件替换进原路径即可。</p>
<h1 id="jie-yu">结语<a href="#jie-yu" class="header-anchor" target="_blank" aria-hidden="true">#</a></h1><p>破解方式至3.1.1依旧有效，不保证未来不会发生变化，仅供参考。</p>
<p>曾经我们上课的时候，使用的startUML的颜值，那真的是一言难尽，可能我校用的还是1.0的上古版本（最主要是教材还对的上），太恐怖了，不过现在startUML 3.x的颜值真的很不错，QAQ虽然这也不是我破解的理由。好了好了，我这条咸鱼又滚回去过我的咸鱼生活惹！告辞~</p>
]]></content>
      <categories>
        <category>摸鱼日常</category>
      </categories>
      <tags>
        <tag>StartUML</tag>
        <tag>破解</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot后端接收LocalDateTime参数最佳实践</title>
    <url>/2019/11/05/SpringBoot-LocalDateTime-%E5%90%8E%E7%AB%AF%E6%8E%A5%E6%94%B6%E5%8F%82%E6%95%B0%E6%9C%80%E4%BD%B3%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h2 id="0x0-localdatetime-zai-springboot-zhong-de-jiong-jing">0x0. LocalDateTime在SpringBoot中的窘境<a href="#0x0-localdatetime-zai-springboot-zhong-de-jiong-jing" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p><strong>问题由来</strong>：在前不久，我们后台就已经抛弃了Date这个类，而改用了java8提供的LocalDateTime，但是正常情况下LocalDateTime的构造函数是私有的，无法像Date那样直接被spring mvc直接处理，所以带给了我很多困扰，甚至有一次项目急迫致使我直接使用String去对时间进行接收，然后再通过DateTimeFormatter在Controller层对参数进行了一层处理，才能将LocalDateTime传给了Service进行处理，直接导致的结果就是方法变得有点丑陋了（逃~</p>
<a id="more"></a>

<h2 id="0x1-chu-bu-jie-jue-localdatetime-zuo-wei-query-can-shu-de-wen-ti">0x1. 初步解决LocalDateTime作为query参数的问题<a href="#0x1-chu-bu-jie-jue-localdatetime-zuo-wei-query-can-shu-de-wen-ti" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>经过多方面查询得知，对于java8时间格式的问题，其实困扰着很多人，网上也提供了很多的解决方案，比如说下面这种在query参数上加注解的方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/api"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestApi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/time"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestInfo <span class="title">getTime</span><span class="params">(@RequestParam</span></span></span><br><span class="line"><span class="function"><span class="params">                            @DateTimeFormat(pattern = <span class="string">"yyyy-MM-dd HH:mm:ss"</span>)</span></span></span><br><span class="line"><span class="function">                                LocalDateTime time) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> RestInfo.ok(time);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请求结果如下：<br><img src="https://ae01.alicdn.com/kf/H41b34ac5e5b948be9c5c318d008dec74R.png" alt="请求结果"></p>
<p>但是这样造成的后果是严重影响了代码的质量，每个使用LocalDateTime的参数的地方都需要使用两个注解。</p>
<h2 id="0x2-xun-zhao-quan-ju-jie-jue-fang-shi">0x2. 寻找全局解决方式<a href="#0x2-xun-zhao-quan-ju-jie-jue-fang-shi" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>接着我又继续尝试了这一种在加了@Configuration的类中使用@Bean注入了一个Convert转换器的方式就是一种很常见的方法（PS：LocalDateTimeUtils是我自己写的一个工具类，后面再提）:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Converter&lt;String, LocalDateTime&gt; <span class="title">localDateTimeConvert</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Converter&lt;String, LocalDateTime&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> LocalDateTime <span class="title">convert</span><span class="params">(String source)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isEmpty(source)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> LocalDateTimeUtils.convert(source.trim());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Controller层代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/api"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestApi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/time"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestInfo <span class="title">getTime</span><span class="params">(LocalDateTime time)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> RestInfo.ok(time);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是我使用过后却并没有得到我想要的效果，出现了报错，这是为什么？<br>得到的报错如下<code>java.lang.IllegalStateException: No primary or default constructor found for class java.time.LocalDateTime</code>，仔细想了一下为什么别人能使用呢，后面才发现自己缺少了一个注解@RequestParam，好的，我这就去加上：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/api"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestApi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/time"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestInfo <span class="title">getTime</span><span class="params">(@RequestParam LocalDateTime time)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> RestInfo.ok(time);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请求结果：<br><img src="https://ae01.alicdn.com/kf/H58bf0648f70749978e2bfa1ccf130e2e8.png" alt="请求结果"></p>
<p>nice！全局方式query也生效了，现在我们来尝试一下使用Model去接收参数：</p>
<p>TestModel:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestModel</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> LocalDateTime time;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Controller层：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/api"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestApi</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/model"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestInfo <span class="title">getModel</span><span class="params">(TestModel model)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> RestInfo.ok(model);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请求结果：<br><img src="https://ae01.alicdn.com/kf/Hfbf7b43a25aa445ab1fccf243d49241bh.png" alt></p>
<p>嗯，现在已经大致满足要求了，但是对于简洁到了极致的自己来说却依旧不能感到满足，为什么LocalDateTime作为一个属性存在于Model中时却不需要使用注解方式去指定呢？而我们不使用@RequestParam时LocalDateTime却会出现报错？</p>
<h2 id="0x3-shen-ru-yan-jiu-fen-xi-can-shu-zhuan-huan-guo-cheng">0x3. 深入研究分析参数转换过程<a href="#0x3-shen-ru-yan-jiu-fen-xi-can-shu-zhuan-huan-guo-cheng" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>我们先在先在Controller层下一个断点，然后查看调用堆栈，找到了一个可疑的方法：invokeForRequest<br><img src="https://ae01.alicdn.com/kf/Hbed78744be2e49c191d8d2d243d55791s.png" alt></p>
<p>我们点进去看一看：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">invokeForRequest</span><span class="params">(NativeWebRequest request, @Nullable ModelAndViewContainer mavContainer,</span></span></span><br><span class="line"><span class="function"><span class="params">        Object... providedArgs)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="comment">// 这个方法应该是获取参数的，跟进方法看一下做了什么</span></span><br><span class="line">    Object[] args = getMethodArgumentValues(request, mavContainer, providedArgs);</span><br><span class="line">    <span class="keyword">if</span> (logger.isTraceEnabled()) &#123;</span><br><span class="line">        logger.trace(<span class="string">"Arguments: "</span> + Arrays.toString(args));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> doInvoke(args);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Object[] getMethodArgumentValues(NativeWebRequest request, <span class="meta">@Nullable</span> ModelAndViewContainer mavContainer,</span><br><span class="line">        Object... providedArgs) <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    <span class="comment">// 获取方法参数列表</span></span><br><span class="line">    MethodParameter[] parameters = getMethodParameters();</span><br><span class="line">    <span class="keyword">if</span> (ObjectUtils.isEmpty(parameters)) &#123;</span><br><span class="line">        <span class="keyword">return</span> EMPTY_ARGS;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Object[] args = <span class="keyword">new</span> Object[parameters.length];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; parameters.length; i++) &#123;</span><br><span class="line">        MethodParameter parameter = parameters[i];</span><br><span class="line">        parameter.initParameterNameDiscovery(<span class="keyword">this</span>.parameterNameDiscoverer);</span><br><span class="line">        args[i] = findProvidedArgument(parameter, providedArgs);</span><br><span class="line">        <span class="keyword">if</span> (args[i] != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="keyword">this</span>.resolvers.supportsParameter(parameter)) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(formatArgumentError(parameter, <span class="string">"No suitable resolver"</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 这里应该进行的是参数绑定操作，跟进</span></span><br><span class="line">            args[i] = <span class="keyword">this</span>.resolvers.resolveArgument(parameter, mavContainer, request, <span class="keyword">this</span>.dataBinderFactory);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            <span class="comment">// Leave stack trace for later, exception may actually be resolved and handled...</span></span><br><span class="line">            <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">                String exMsg = ex.getMessage();</span><br><span class="line">                <span class="keyword">if</span> (exMsg != <span class="keyword">null</span> &amp;&amp; !exMsg.contains(parameter.getExecutable().toGenericString())) &#123;</span><br><span class="line">                    logger.debug(formatArgumentError(parameter, exMsg));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">throw</span> ex;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> args;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">resolveArgument</span><span class="params">(MethodParameter parameter, @Nullable ModelAndViewContainer mavContainer,</span></span></span><br><span class="line"><span class="function"><span class="params">        NativeWebRequest webRequest, @Nullable WebDataBinderFactory binderFactory)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="comment">// 继续跟进，方法就紧跟在下边</span></span><br><span class="line">    HandlerMethodArgumentResolver resolver = getArgumentResolver(parameter);</span><br><span class="line">    <span class="keyword">if</span> (resolver == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Unsupported parameter type ["</span> +</span><br><span class="line">                parameter.getParameterType().getName() + <span class="string">"]. supportsParameter should be called first."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> resolver.resolveArgument(parameter, mavContainer, webRequest, binderFactory);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> HandlerMethodArgumentResolver <span class="title">getArgumentResolver</span><span class="params">(MethodParameter parameter)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 在springboot 2.2.0 时这里会直接将LocalDateTime的解析器确定为RequestParamMethodArgumentResolver</span></span><br><span class="line">    HandlerMethodArgumentResolver result = <span class="keyword">this</span>.argumentResolverCache.get(parameter);</span><br><span class="line">    <span class="keyword">if</span> (result == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 这里argumentResolvers是注册的参数解析器列表</span></span><br><span class="line">        <span class="keyword">for</span> (HandlerMethodArgumentResolver resolver : <span class="keyword">this</span>.argumentResolvers) &#123;</span><br><span class="line">            <span class="keyword">if</span> (resolver.supportsParameter(parameter)) &#123;</span><br><span class="line">                result = resolver;</span><br><span class="line">                <span class="keyword">this</span>.argumentResolverCache.put(parameter, result);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们展开参数解析器的列表：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">this.argumentResolvers = &#123;LinkedList@6957&#125;  size = 26</span><br><span class="line"> 0 = &#123;RequestParamMethodArgumentResolver@6959&#125; </span><br><span class="line"> 1 = &#123;RequestParamMapMethodArgumentResolver@7055&#125; </span><br><span class="line"> 2 = &#123;PathVariableMethodArgumentResolver@7056&#125; </span><br><span class="line"> 3 = &#123;PathVariableMapMethodArgumentResolver@7057&#125; </span><br><span class="line"> 4 = &#123;MatrixVariableMethodArgumentResolver@7058&#125; </span><br><span class="line"> 5 = &#123;MatrixVariableMapMethodArgumentResolver@7059&#125; </span><br><span class="line"> 6 = &#123;ServletModelAttributeMethodProcessor@7060&#125; </span><br><span class="line"> 7 = &#123;RequestResponseBodyMethodProcessor@7061&#125; </span><br><span class="line"> 8 = &#123;RequestPartMethodArgumentResolver@7062&#125; </span><br><span class="line"> 9 = &#123;RequestHeaderMethodArgumentResolver@7063&#125; </span><br><span class="line"> 10 = &#123;RequestHeaderMapMethodArgumentResolver@7064&#125; </span><br><span class="line"> 11 = &#123;ServletCookieValueMethodArgumentResolver@7065&#125; </span><br><span class="line"> 12 = &#123;ExpressionValueMethodArgumentResolver@7066&#125; </span><br><span class="line"> 13 = &#123;SessionAttributeMethodArgumentResolver@7067&#125; </span><br><span class="line"> 14 = &#123;RequestAttributeMethodArgumentResolver@7068&#125; </span><br><span class="line"> 15 = &#123;ServletRequestMethodArgumentResolver@7069&#125; </span><br><span class="line"> 16 = &#123;ServletResponseMethodArgumentResolver@7070&#125; </span><br><span class="line"> 17 = &#123;HttpEntityMethodProcessor@7071&#125; </span><br><span class="line"> 18 = &#123;RedirectAttributesMethodArgumentResolver@7072&#125; </span><br><span class="line"> 19 = &#123;ModelMethodProcessor@7073&#125; </span><br><span class="line"> 20 = &#123;MapMethodProcessor@7074&#125; </span><br><span class="line"> 21 = &#123;ErrorsMethodArgumentResolver@7075&#125; </span><br><span class="line"> 22 = &#123;SessionStatusMethodArgumentResolver@7076&#125; </span><br><span class="line"> 23 = &#123;UriComponentsBuilderMethodArgumentResolver@7077&#125; </span><br><span class="line"> 24 = &#123;RequestParamMethodArgumentResolver@7078&#125; </span><br><span class="line"> 25 = &#123;ServletModelAttributeMethodProcessor@7079&#125;</span><br></pre></td></tr></table></figure>

<p>是不是看到了几个看着很眼熟的东西？<code>RequestParam</code>,<code>PathVariable</code>,<code>RequestBody</code>，在idea中查找RequestParamMethodArgumentResolver类，进入他的方法看一下，</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supportsParameter</span><span class="params">(MethodParameter parameter)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (parameter.hasParameterAnnotation(RequestParam.class)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (Map.class.isAssignableFrom(parameter.nestedIfOptional().getNestedParameterType())) &#123;</span><br><span class="line">            <span class="comment">// RequestParam眼熟不，getParameterAnnotation，对应的就是@RequestParam注解</span></span><br><span class="line">            RequestParam requestParam = parameter.getParameterAnnotation(RequestParam.class);</span><br><span class="line">            <span class="keyword">return</span> (requestParam != <span class="keyword">null</span> &amp;&amp; StringUtils.hasText(requestParam.name()));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (parameter.hasParameterAnnotation(RequestPart.class)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        parameter = parameter.nestedIfOptional();</span><br><span class="line">        <span class="keyword">if</span> (MultipartResolutionDelegate.isMultipartArgument(parameter)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">this</span>.useDefaultResolution) &#123;</span><br><span class="line">            <span class="keyword">return</span> BeanUtils.isSimpleProperty(parameter.getNestedParameterType());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里可以基本确定一件事，在我这测试时默认的参数解析器有26个，每个参数解析器有着各自的用途，比如加了@RequestParam注解的参数会被RequestParamMethodArgumentResolver所解析，然后再查找类型有无相应的Convert转换器，在我们这里使用@Bean注入了一个Convert转换器，所以LocalDateTime能被正确的解析。</p>
<p>那么我们之前不加@RequestParam直接使用LocalDateTime作为参数的时候是被哪个解析器所捕获的呢，方法已经找到了，那么直接下个断点跑一遍查看result返回的是哪个解析器就知道了！<br><img src="https://ae01.alicdn.com/kf/H9dadf5167f2c4916b892b073f713c112B.png" alt></p>
<p>如果没记错ServletModelAttributeMethodProcessor是参数解析器的最后一个，我们进入到这个类看一眼，没有supportsParameter方法，但是他继承了ModelAttributeMethodProcessor，我们再进入他的父类中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supportsParameter</span><span class="params">(MethodParameter parameter)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (parameter.hasParameterAnnotation(ModelAttribute.class) ||</span><br><span class="line">            (<span class="keyword">this</span>.annotationNotRequired &amp;&amp; !BeanUtils.isSimpleProperty(parameter.getParameterType())));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于没有指定参数解析器的参数来说，默认指定的参数解析器是ModelAttributeMethodProcessor，并且由源码得知，如果加了@ModelAttribute注解，或者非简单属性则会被该解析器捕获，所以我们平时所使用的model去接收不需要加注解即可被正确的解析，既然都来了，那么顺便看一下resolveArgument方法是怎么解析参数的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> Object <span class="title">resolveArgument</span><span class="params">(MethodParameter parameter, @Nullable ModelAndViewContainer mavContainer,</span></span></span><br><span class="line"><span class="function"><span class="params">        NativeWebRequest webRequest, @Nullable WebDataBinderFactory binderFactory)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">    <span class="keyword">if</span> (mavContainer.containsAttribute(name)) &#123;</span><br><span class="line">        attribute = mavContainer.getModel().get(name);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// Create attribute instance</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 这里是对参数的绑定构建了，点进去看一眼</span></span><br><span class="line">            attribute = createAttribute(name, parameter, binderFactory, webRequest);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (BindException ex) &#123;</span><br><span class="line">            <span class="keyword">if</span> (isBindExceptionRequired(parameter)) &#123;</span><br><span class="line">                <span class="comment">// No BindingResult parameter -&gt; fail with BindException</span></span><br><span class="line">                <span class="keyword">throw</span> ex;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Otherwise, expose null/empty value and associated BindingResult</span></span><br><span class="line">            <span class="keyword">if</span> (parameter.getParameterType() == Optional.class) &#123;</span><br><span class="line">                attribute = Optional.empty();</span><br><span class="line">            &#125;</span><br><span class="line">            bindingResult = ex.getBindingResult();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>继续跟进createAttribute方法，看看他是怎么将值绑定给对象的：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">createAttribute</span><span class="params">(String attributeName, MethodParameter parameter,</span></span></span><br><span class="line"><span class="function"><span class="params">        WebDataBinderFactory binderFactory, NativeWebRequest webRequest)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    MethodParameter nestedParameter = parameter.nestedIfOptional();</span><br><span class="line">    Class&lt;?&gt; clazz = nestedParameter.getNestedParameterType();</span><br><span class="line"></span><br><span class="line">    Constructor&lt;?&gt; ctor = BeanUtils.findPrimaryConstructor(clazz);</span><br><span class="line">    <span class="keyword">if</span> (ctor == <span class="keyword">null</span>) &#123;</span><br><span class="line">        Constructor&lt;?&gt;[] ctors = clazz.getConstructors();</span><br><span class="line">        <span class="keyword">if</span> (ctors.length == <span class="number">1</span>) &#123;</span><br><span class="line">            ctor = ctors[<span class="number">0</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                ctor = clazz.getDeclaredConstructor();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">catch</span> (NoSuchMethodException ex) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"No primary or default constructor found for "</span> + clazz, ex);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Object attribute = constructAttribute(ctor, attributeName, parameter, binderFactory, webRequest);</span><br><span class="line">    <span class="keyword">if</span> (parameter != nestedParameter) &#123;</span><br><span class="line">        attribute = Optional.of(attribute);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> attribute;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>看到这里是不是发现报错的地方有点熟悉？这就是我们之前LocalDateTime抛出异常的地方了，可以得知对于对象的绑定，先通过BeanUtils获取主要的构造函数，如果获取不到，则使用反射的方式先尝试获取声明为public的无参构造函数，最后才会尝试使用getDeclaredConstructor获取所有的无参构造函数，但是对于LocalDateTime这种使用工厂构造不存在无参构造函数的类来说就会直接抛出NoSuchMethodException异常。那么如果我们不想使用@RequestParam注解加在参数上怎么办呢？</p>
<h2 id="0x4-chuang-jian-can-shu-jie-xi-qi">0x4. 创建参数解析器<a href="#0x4-chuang-jian-can-shu-jie-xi-qi" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>如果我们不希望在LocalDateTime上增加注解，然后再通过RequestParamMethodArgumentResolver解析，我们可以自己定制一种针对LocalDateTime的参数解析器，通过继承WebMvcConfigurer重写addArgumentResolvers方法，然后add一个自定义的HandlerMethodArgumentResolver即可解决刚才的问题，但是在2.2.0版本上会自动指定LocalDateTime的解析器原因未知（我太蔡了…找不到是怎么做到的，想看看他是怎么获取的，但是发现那个方法再无限调用，完全没有头绪。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MvcConfig</span> <span class="keyword">implements</span> <span class="title">WebMvcConfigurer</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 增加 &#123;<span class="doctag">@link</span> LocalDateTime&#125; 的自定义参数解析器，不需要用注解方式指定query参数即可解析</span></span><br><span class="line"><span class="comment">     * 2.2.0版本后较大改变，自动确定LocalDateTime解析器为 &#123;<span class="doctag">@link</span></span></span><br><span class="line"><span class="comment">     * org.springframework.web.method.annotation.RequestParamMethodArgumentResolver&#125;</span></span><br><span class="line"><span class="comment">     * 但是依旧可以注册使用本解析器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addArgumentResolvers</span><span class="params">(List&lt;HandlerMethodArgumentResolver&gt; resolvers)</span> </span>&#123;</span><br><span class="line">        resolvers.add(<span class="keyword">new</span> LocalDateArgumentResolverHandler());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">LocalDateArgumentResolverHandler</span> <span class="keyword">implements</span> <span class="title">HandlerMethodArgumentResolver</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">supportsParameter</span><span class="params">(MethodParameter parameter)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> parameter.getParameterType().equals(LocalDateTime.class);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">resolveArgument</span><span class="params">(MethodParameter parameter, ModelAndViewContainer mavContainer,</span></span></span><br><span class="line"><span class="function"><span class="params">                                      NativeWebRequest webRequest, WebDataBinderFactory binderFactory)</span> </span>&#123;</span><br><span class="line">            String param = webRequest.getParameter(Objects.requireNonNull(parameter.getParameterName()));</span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isEmpty(param)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> LocalDateTimeUtils.convert(param.trim());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="0x5-tui-jian-shi-yong-zhu-ce-convert-de-fang-shi">0x5. 推荐使用注册Convert的方式<a href="#0x5-tui-jian-shi-yong-zhu-ce-convert-de-fang-shi" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>相比于注入@Bean的方式创建Convert的方式，我个人更喜欢使用继承WebMvcConfigurer后重写addFormatters方法来注册自定义Convert。这种方式相比于@Bean注入，更容易让人理解，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MvcConfig</span> <span class="keyword">implements</span> <span class="title">WebMvcConfigurer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addFormatters</span><span class="params">(FormatterRegistry registry)</span> </span>&#123;</span><br><span class="line">        registry.addConverter(<span class="keyword">new</span> LocalDateTimeConverter());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">LocalDateTimeConverter</span> <span class="keyword">implements</span> <span class="title">Converter</span>&lt;<span class="title">String</span>, <span class="title">LocalDateTime</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> LocalDateTime <span class="title">convert</span><span class="params">(String source)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isEmpty(source)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> LocalDateTimeUtils.convert(source.trim());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="0x6-xiao-jie">0x6. 小结<a href="#0x6-xiao-jie" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>对于request参数其实还有initBinder，Formatter等方式进行处理，这里就不一一贴出来了，有兴趣的各位可以在网上查询相关资料。还有jackson相关的序列化，通过继承com.fasterxml.jackson.databind.JsonDeserializer和com.fasterxml.jackson.databind.JsonSerializer生成自定义的Jackson Deserializer和Serializer，然后在@Configuration注解的配置类内注入@Bean将Jackson Module注册为@Bean，SpringBoot会自动注入进ObjectMapper中。另一种方式是自己注入一个自己定制的ObjectMapper为@Bean，然后将Module注入进ObjectMapper中。甚至可以通过定制转换规则，从而使类型支持多种参数。</p>
<h2 id="0xf-fu-lu">0xf. 附录<a href="#0xf-fu-lu" class="header-anchor" target="_blank" aria-hidden="true">#</a></h2><p>支持秒时间戳，毫秒时间戳，自定义时间格式<code>yyyy-MM-dd HH:mm[:ss][.sss]</code>，ISO标准时间<code>yyyy-MM-ddTHH:mm[:ss][.sss]</code>，UTC标准时间<code>yyyy-MM-ddTHH:mm:ss[.sss]Z</code>，甚至可以支持更多种类型，但是考虑到时间复杂度，感觉这已经不能再简化了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> teble</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2019/11/5 10:43</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@description</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LocalDateTimeUtils</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">static</span> String REGEX_TIME = <span class="string">"^(\\d&#123;10,13&#125;|\\d&#123;4&#125;-\\d&#123;2&#125;-\\d&#123;2&#125;.\\d&#123;2&#125;:\\d&#123;2&#125;.*)$"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> LocalDateTime <span class="title">convert</span><span class="params">(String resolver)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (Pattern.matches(REGEX_TIME, resolver)) &#123;</span><br><span class="line">            Instant instant;</span><br><span class="line">            <span class="keyword">switch</span> (resolver.length()) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="number">10</span>:</span><br><span class="line">                    instant = Instant.ofEpochSecond(Long.parseLong(resolver));</span><br><span class="line">                    <span class="keyword">return</span> LocalDateTime.ofInstant(instant, ZoneId.of(<span class="string">"GMT+8"</span>));</span><br><span class="line">                <span class="keyword">case</span> <span class="number">13</span>:</span><br><span class="line">                    instant = Instant.ofEpochMilli(Long.parseLong(resolver));</span><br><span class="line">                    <span class="keyword">return</span> LocalDateTime.ofInstant(instant, ZoneId.of(<span class="string">"GMT+8"</span>));</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (resolver.endsWith(<span class="string">"Z"</span>)) &#123;</span><br><span class="line">                <span class="keyword">return</span> LocalDateTime.ofInstant(Instant.parse(resolver), ZoneId.of(<span class="string">"GMT+8"</span>));</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (resolver.charAt(<span class="number">10</span>) == <span class="string">'T'</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> LocalDateTime.parse(resolver, DateTimeFormatter.ISO_LOCAL_DATE_TIME);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (resolver.charAt(<span class="number">10</span>) == <span class="string">' '</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> LocalDateTime.parse(resolver, <span class="keyword">new</span> DateTimeFormatterBuilder()</span><br><span class="line">                    .parseCaseInsensitive()</span><br><span class="line">                    .append(ISO_LOCAL_DATE)</span><br><span class="line">                    .appendLiteral(<span class="string">' '</span>)</span><br><span class="line">                    .append(ISO_LOCAL_TIME)</span><br><span class="line">                    .toFormatter());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
        <tag>Java</tag>
        <tag>后端</tag>
      </tags>
  </entry>
  <entry>
    <title>博客终于搭好了</title>
    <url>/2019/10/31/%E5%8D%9A%E5%AE%A2%E7%BB%88%E4%BA%8E%E6%90%AD%E5%A5%BD%E4%BA%86/</url>
    <content><![CDATA[<p>博客搭好其实也有好几天了，但是被耽搁了一阵子，虽然最近很忙，但是还是下定决心来写一些东西，方便自己也方便他人。<br>其实很多时候都想记录一些容易忘记的东西，但是总觉得写博客太麻烦，所以一拖再拖。现实习中，java后台一枚，开发使用SpringBoot，一起来学习吧！<br><img src="https://ae01.alicdn.com/kf/H87e9ec5282ad4ea08728ed6ab001fc0dC.jpg" alt></p>
<a id="more"></a>
]]></content>
      <categories>
        <category>摸鱼日常</category>
      </categories>
  </entry>
</search>
